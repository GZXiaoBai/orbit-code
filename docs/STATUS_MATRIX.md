# Orbit Code Status Matrix

最近审查日期：2026-06-06

本文档是当前事实源。旧自研运行时、实验适配线和旧 session 恢复路线已经删除或降级为历史，不再作为产品状态依据。

## 验证基线

| 命令 | 状态 | 说明 |
| --- | --- | --- |
| `npm test -- --run` | `verified` | 2026-06-06 通过，47 files / 224 tests；新增覆盖 Plan follow-up 线程上下文、accepted Plan Build prompt、Build idle auto-recover、final summary 解锁、terminal operation 防降级、nested app-server usage、session persistence debounce、unknown-wrapper app-server usage、Settings Beta gate。 |
| `npm run build` | `verified` | 2026-06-06 通过，仅保留既有 Vite chunk size / dynamic import warning。当前前端以 Codex sidecar projection 驱动 Thread、Review Dock、Action overlay 和 runtime settings；Plan/普通聊天 route 为 `direct-deepseek-plan`，Build route 为 `codex-app-server-build`。 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | `verified` | 2026-06-06 通过，78 lib tests + AST/code graph/embedding integration tests；包含 Codex JSON-RPC、Responses bridge、direct Plan、app-server cleanup/recover、workspace patch stale-write 防护和 provider/vault/parser 回归。 |
| `npm run test:e2e` | `verified` | 2026-06-06 通过，46 passed；Playwright fixture 已迁到 Codex thread/turn/item，并覆盖桌面 Plan direct route、Settings recover input state、Build fixture approval/terminal/file edit/usage、pending approval/question/patch reload recovery、deny path 和 provider gating。 |
| `npm run smoke:desktop-readiness` | `verified on Ubuntu CI / blocked on macOS` | 2026-06-06 CI run `27004464759` 通过：Ubuntu `tauri-driver`、`WebKitWebDriver`、DBus/Xvfb、debug no-bundle build、packaged desktop Plan window smoke 和 packaged Build readiness smoke 全部成功。Plan smoke runner 在退出前等待并强制清理 tauri-driver，避免 blocked/non-live 路径写完报告后被后台进程拖到 workflow timeout。macOS 本机仍因 WKWebView 无 WebDriver 写入 blocked 报告。 |
| `npm run smoke:desktop-plan` | `verified launch smoke on Ubuntu CI / blocked on macOS` | 2026-06-06 CI run `27004464759` 通过 packaged desktop Plan window launch smoke：真实 Tauri binary 渲染 workbench shell；默认不启用 live Plan submit，避免依赖本地 vault/API Key。设置 `ORBIT_DESKTOP_PLAN_LIVE=1` 后仍可进一步在真实窗口发送“你好”并检查没有 app-server 错误。 |
| `npm run smoke:desktop-build` | `verified live approve/deny on Ubuntu CI / blocked on macOS` | 2026-06-06 CI run `27004464759` 的 credentialed `desktop-build-live` job 通过 approve 和 deny 两条真实 DeepSeek Build 路径：vault bootstrap、DeepSeek readiness、debug desktop build、approval submit、follow-up approval、terminal output、file edit、usage evidence、final summary、deny path 和 artifact upload 均为 success。CI artifact 已同步到 `docs/smoke/latest-tauri-webdriver-build-approve-smoke.json`、`docs/smoke/latest-tauri-webdriver-build-deny-smoke.json` 和 `docs/smoke/latest-tauri-webdriver-build-smoke.json`。前端递归提取 app-server usage metadata，并在收到 usage item 但数值暂不可解析时显示 observed usage strip。macOS 本机仍因 WKWebView 无 WebDriver 写入 blocked 报告。 |
| `npm run smoke:plan` | `verified` | 2026-06-06 通过，写入 `docs/smoke/latest-plan-stability-smoke.json`。该入口串联 `smoke:plan-route` 和 `smoke:plan-live`，作为普通聊天/Plan 的稳定门禁：既验证 workbench 提交 route 不触碰 Codex app-server，也验证真实 DeepSeek SSE 能返回增量。 |
| `npm run smoke:plan-route` | `verified` | 2026-06-06 通过，写入 `docs/smoke/latest-plan-route-smoke.json`。该入口封装聚焦 Playwright smoke，验证普通 Plan/chat 提交会调用 `codex_turn_start` + `runtimeMode=direct-deepseek-plan`，且发送后不调用 `codex_sidecar_status` 或 `codex_runtime_restart`。 |
| `npm run smoke:plan-live` | `verified` | 2026-06-06 通过，写入 `docs/smoke/latest-plan-live-streaming.json`。该入口从 Orbit trusted-device vault 内存解锁 DeepSeek credential，直接调用 DeepSeek chat SSE，验证 “你好” 可以返回 reasoning/content 增量且不经过 Codex app-server；不打印、不写入 API key。 |
| `npm run smoke:deepseek` | `contract smoke` | 2026-06-06 通过。默认入口执行 prepared Codex sidecar + app-server routing contract smoke，并写入 `docs/smoke/latest-codex-app-server-deepseek-smoke.json`。translation-only smoke 是 `npm run smoke:deepseek:bridge-unit`；旧直连 DeepSeek harness 是 `npm run smoke:deepseek:legacy`。 |
| `npm run smoke:deepseek:live-readiness` | `verified readiness` | 2026-05-31 通过，写入 `docs/smoke/latest-codex-live-readiness.json`。该入口验证 trusted-device vault 可非交互解锁、DeepSeek credential 存在、prepared Codex sidecar 存在、DeepSeek `/v1/models` 可达；不打印、不写入、不通过 env 传递 API key。它是 live Build smoke 的前置证据，不代表 approval -> terminal/fileEdit -> final summary 已完成。 |
| `npm run smoke:deepseek:live-app-server` | `verified live smoke` | 2026-05-31 通过并写入 `docs/smoke/latest-codex-live-app-server.json`。该入口启动真实 prepared Codex app-server、本地 Node Responses bridge、trusted-device vault DeepSeek credential，并验证 initialize、thread/start、DeepSeek live stream、approval request/response、terminal output、临时 workspace file edit、usage update、final assistant summary 和 turn completion。 |
| `npm run tauri build -- --debug --no-bundle` | `verified` | 2026-06-06 本地通过，beforeBuildCommand 会执行 `prepare:codex-sidecar` 和前端 build，并生成 `src-tauri/target/debug/orbit-code`。CI WebDriver smoke 使用该入口获得 Tauri CLI 构建语义但跳过 app/dmg/deb/msi bundling；生成的 `src-tauri/binaries/codex-*` 只作为本地 ignored artifact，不进入 Git。 |
| CI release build matrix | `verified` | 2026-06-06 CI run `27004464759` 通过 macOS DMG、Windows MSI 和 Ubuntu Linux `.deb` release build，并上传对应 artifacts。Linux release target 固定为 `deb`，避免 AppImage packaging 在 CI 中长时间挂住。 |
| `codex exec` mock Responses SSE probe | `verified` | 2026-05-30 使用本机 `codex-cli 0.135.0-alpha.1` 指向 loopback mock `/v1/responses`，Codex 接受 `response.created`、`response.output_item.done`、`response.completed` SSE frames 并产出 `item.completed`/`turn.completed`。 |

## Runtime Matrix

| 区域 | 状态 | 当前事实 | 下一步 |
| --- | --- | --- | --- |
| Codex sidecar lifecycle | `verified for Beta` | Rust Codex command prepares a pinned sidecar, starts one persistent `codex app-server --listen stdio://`, generates no-secret temporary config, routes JSON-RPC responses through pending channels, maps app-server notifications to fixed Tauri events, and caches `Orbit threadId -> Codex threadId` for the sidecar lifetime. The former monolithic `src-tauri/src/commands/codex.rs` is now a 12-line façade with physical shards under `src-tauri/src/commands/codex/` for types/state, JSON-RPC, Responses bridge, direct Plan, app-server lifecycle, notification mapping, turn commands, response translation, and tests. Build turns use Codex `untrusted` approval policy so command execution goes through approval requests. CI run `27004464759` proves the packaged desktop path reaches approval submit, follow-up approval, server request resolution, token usage updates, final summary, file write, deny path, operation completion, and artifact upload. App-server failure/recover cleanup clears stdin/child/thread cache/active context and releases pending response/request state; active Build operations waiting on approval are cancelled for user recovery, completed operations are preserved through later cleanup, and final app-server errors upsert a stable per-turn error card. Runtime diagnostics track no-secret app-server stage history tail covering spawn/initialize/app-server-ready/thread/start/turn/start/request/response/notification/server-request/pending-request/approval-submit/timeout/operation status/cleanup. | Keep expanding deterministic sidecar crash/restart fixtures, but do not block Beta on alternative Agent or provider work. |
| Orbit provider bridge | `verified for DeepSeek Beta` | Bridge 绑定 `127.0.0.1:0` 并提供基础 HTTP loop：`GET /v1/models`、`POST /v1/responses`。DeepSeek `/v1/responses` 从 vault 内存读取 key，完成 history sanitization、DeepSeek chat completion translation、SSE reasoning/content/tool/usage frames。Rust bridge has pure parser coverage for streamed DeepSeek reasoning/content/tool_call/usage aggregation, command-layer coverage verifies Codex orphan tool outputs, `tool_result`, `tool_call_output`, and unknown roles are converted into DeepSeek-safe user messages instead of illegal provider roles, SSE error frames fail the stream with an explicit provider error instead of completing with partial content, and provider error text is redacted before unlocked API keys can reach UI diagnostics or logs. OpenRouter、Qwen/DashScope、SiliconFlow、Kimi、Groq、自定义 OpenAI-compatible 均有 catalog 和 blocked reason。CI run `27004464759` proves credentialed packaged desktop Build approve/deny with DeepSeek through Orbit vault, Rust bridge, Codex app-server, terminal/file edit/usage/final summary, and artifact upload. macOS local run remains blocked by WebDriver limits rather than runtime failure. | Keep DeepSeek as the only Build provider until each additional bridge has equivalent live desktop evidence. |
| Frontend runtime | `verified for Beta` | `useWorkspace.ts` remains a workbench facade and no longer imports old runner/tool loop. `useCodexSession` consumes `AgentRuntimePort`, event notifications, sidecar version info, restart result, recover result, and runtime settings model. Plan/ordinary chat uses the direct provider streaming route (`direct-deepseek-plan`) and does not preflight, restart, or initialize the Codex app-server. Plan follow-up prompts include recent same-thread context for short requests like “开始吧”, accepted Plan drafts are persisted per thread in the `codex-sidecar.v1` session, and Build prompts inject only explicitly accepted Plans while direct YAML imports remain task-selection context. Build is the only Codex app-server route; effective Build gate and submitted turn agree on sidecar/provider/vault/model blocked reasons. Composer lock is derived from runtime/active-operation state, terminal states restore submission, stale sidecar connection events are ignored, app-server `appTurnId` events are accepted for the same active Build thread, late running patches cannot downgrade terminal operations, final summary and `turn/completed` unlock input immediately, and idle auto-recover restores composer without writing misleading Codex errors. Settings runtime control plane now surfaces Beta gate, production Codex Build core, sidecar version/path/sha256, pid, bridge base URL, diagnostics, restart/recover, model discovery, bridge smoke, latest desktop Build smoke, Build gate reason, and alternative-agent spike/blocked status. | Keep broadening streaming/retry/reload recovery tests as follow-up; do not reopen old runner/tool loop. |
| Thread projection | `verified for fixture loop` | `CodexItem -> RuntimeMessage / ThreadEvent / ActionRequired / TerminalRun / RunStep` 已覆盖 planDraft、fileEdit、usage、approval denied、question answered。 | 跟随 Codex app-server 原生 item schema 做兼容映射。 |
| Session schema | `verified for first pass` | `sessionStore` 改为 `codex-sidecar.v1`，旧 schema 不参与运行时恢复；E2E 覆盖 legacy session 不回放旧 runtime。 | 增加 legacy unsupported 的删除/归档 UX 打磨。 |
| Review Dock | `verified for Beta` | Review Dock 消费 Codex projection，E2E 覆盖 command approval、terminal、file edit、deny path 和 usage projection。Token usage strip 固定显示在 tab 下方，不再只在 Actions tab 可见，避免 live Build 自动切到 Terminal tab 后隐藏 usage evidence；strip 文案显式包含 `Token usage`/`Token 使用量`，并暴露 `data-codex-usage-total`，便于 packaged UI smoke 稳定识别真实 usage。Week 2 已补 file preview、approval/question、patch conflict、terminal output、usage pending 和 recoverable error 的可读空态/恢复态文案。 | 发布前补一轮真实人工截图即可，不阻塞 Beta 自动化门禁。 |
| Provider registry | `partial` | 继续负责 metadata、model discovery、vault key mapping；Build gating 目前只放行 DeepSeek 和 fixture，其中 DeepSeek 还要求 vault unlock、model discovery、bridge smoke passed，并在 Settings runtime panel 中把 sidecar not ready 纳入 blocked reason。Rust bridge provider catalog 现在覆盖 OpenAI、Anthropic、Gemini、OpenRouter、xAI、Mistral、Groq、Qwen/DashScope、Kimi、SiliconFlow、Zhipu、Together AI、Fireworks AI、Cerebras、NVIDIA NIM、Azure OpenAI、Ollama 和 custom OpenAI-compatible；除 DeepSeek 外全部带明确 Build blocked reason。Ollama 现在也是 `LLMProvider`，legacy local `/api/chat` 非 Build 调用不再错误要求 vault key。Custom OpenAI-compatible 已支持用户填写 Base URL 后导入模型，Together AI、Fireworks AI、Cerebras、NVIDIA NIM 和 Azure OpenAI 已作为 OpenAI-compatible 端点加入 discovery-only 导入路径；远端 Base URL 必须 HTTPS，HTTP 仅允许 loopback；Azure OpenAI Base URL 必须是 HTTPS 且 host 属于 `*.openai.azure.com` 或 `*.services.ai.azure.com`，请求使用 `api-key` header；模型列表解析现在兼容 OpenAI/Anthropic `data`、常见私有网关 `models`、single model object、Google `supportedGenerationMethods` 过滤和 Ollama `name` 形态；新增结构化 `list_llm_model_infos` 导入路径，可保留 API 返回的 context window、max output、tool calling 等非敏感能力 metadata，同时前端仍强制沿用 DeepSeek-only Build gate；Settings 模型列表现在把上下文窗口、输出上限、工具调用、本地/API/官方来源显示为 compact badges，900px Playwright 检查无横向溢出；Base URL 粘贴完整 `/chat/completions`、`/responses`、`/messages`、`/api/chat`、`/models` 端点时会按导入或聊天目标自动归一化。 | 逐个验证 OpenAI/OpenRouter/Qwen/SiliconFlow/Kimi/Groq/Zhipu/Together/Fireworks/Cerebras/NVIDIA/Azure/custom 后解除 Build block。 |

## Cleanup Matrix

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 旧 runtime 文件删除 | `verified for source` | 旧 runner/tool-loop/实验适配器/tool-registry/policy-engine 文件已从 `src/` 删除。 |
| 旧 runtime import boundary | `verified for source` | 新 `codexRuntimeBoundary` 测试扫描生产 TS/TSX，防止旧核心符号回流。 |
| `useWorkspace.ts` 收敛 | `verified for first pass` | 当前 378 行，作为 workbench facade，不再直接执行模型 tool branching。 |
| 前端 tool JSON parsing | `verified for first pass` | 旧 raw tool envelope parser 已删除；UI 只消费 Codex projection。 |
| 旧 session restore | `verified for first pass` | 旧 snapshot/Promise/action replay 不再进入新 session store。 |
| 文档路线 | `verified for status source` | `STATUS_MATRIX.md` 已切换到 Codex sidecar 矩阵，并移除旧路线表述。 |
| Smoke 证据收口 | `verified for cleanup` | Smoke 脚本默认覆盖 `docs/smoke/latest-*.json`，不再生成无限 timestamp 历史；`ORBIT_SMOKE_KEEP_HISTORY=1` 时才额外写历史 JSON，且历史 JSON 被 `.gitignore` 忽略。旧 tracked timestamp smoke 已从工作树删除，准备归入 `smoke-docs-cleanup` 提交边界。 |

## Risk Register

| 风险 | 状态 | 处理 |
| --- | --- | --- |
| Codex app-server protocol drift | `open` | Codex binary/version 已 pin 到 bundled sidecar 路线；Rust 现在有 JSON-RPC 2.0 outbound normalization、JSON-RPC multiplexing、fake app-server lifecycle response/notification/server-request interleaving fixture，以及 app-server notification protocol method classification fixture，覆盖 initialize/thread/start/turn response、turn started/completed notification、assistant delta、approval server request、token usage、warning/error 等关键 Orbit 分类。2026-06-04 本地用 bundled Codex `app-server generate-ts --experimental` 与 `generate-json-schema --experimental` 核对当前 approval response 形状，确认 command/file approval 仍是 `decision: "accept" | "decline"`，permissions approval 是 granted permission profile + scope。下一步仍是把协议导出转成 repo 内 fixture/schema pin，并扩成 fake app-server stdio lifecycle tests，验证 reader loop 对完整 stdout 序列和 pending response cleanup 的端到端行为。 |
| Provider bridge correctness | `mitigated` | DeepSeek HTTP loop 已补 Responses SSE 包装、history sanitization、streamed reasoning/content/tool_call/usage aggregation、tool result repair、mid-stream error rejection 和 provider secret redaction 单测，并通过 prepared sidecar contract smoke；Node live bridge 已用真实 DeepSeek credential 跑通 approval-required Codex app-server Build。Plan 稳定路径已从 app-server 解耦，`smoke:plan-live` 已验证普通 “你好” 聊天可以通过 DeepSeek SSE 增量返回，避免普通对话被 Build runtime 故障拖垮。CI run `27004464759` 已通过桌面 UI + Rust bridge + Codex app-server + DeepSeek 的 packaged approve/deny live Build。 |
| E2E fixture drift | `mitigated` | Playwright fixture 已迁到 Codex item fixture；后续风险是 fixture 与真实 app-server item schema 漂移。 |
| Smoke parity | `verified for DeepSeek Beta` | `smoke:deepseek` 默认含义已切到 prepared sidecar + app-server routing contract smoke；`smoke:deepseek:live-readiness` 已证明本机可通过 trusted-device vault 非交互读取 DeepSeek credential 并访问 DeepSeek `/v1/models`，且 key 不进入 env/config/log。`smoke:deepseek:live-app-server` 已证明真实 Codex app-server + DeepSeek 可以完成 approval request/response、terminal output、usage、final summary 和临时 workspace 文件写入。`desktop-build-live` CI 已把同等能力落到 Rust bridge + packaged desktop UI approve/deny 路径。旧直连 DeepSeek harness 仅作为历史 artifact。 | 后续 smoke parity 风险主要来自新增 provider 或 Codex protocol drift。 |
