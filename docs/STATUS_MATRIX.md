# Orbit Code Status Matrix

Last audited: 2026-05-30.

This file is the current source of truth for implementation status. Historical handoff documents may contain optimistic or obsolete claims.

## Status Legend

| Status | Meaning |
| --- | --- |
| `verified` | Implemented and covered by passing local verification. |
| `partial` | Implemented enough to inspect or use, but missing coverage, edge cases, or integration. |
| `broken` | Implemented or tested, but currently failing or contract mismatch exists. |
| `design-only` | Described in docs/specs, not reliably wired into product flow. |
| `not-started` | No meaningful implementation found. |

## Verification Snapshot

| Command | Result | Notes |
| --- | --- | --- |
| `npm test -- --run` | `verified` | 260 Vitest tests passed, including pure `ThreadEventProtocol` coverage, `ToolCallLifecycle` coverage, non-React `AgentRunKernel` Build-turn preparation, structured `AgentTurnRunner` Build-turn results with injected engine adapters and waiting-action/resume result shape, `ToolLoopController`/`AgentTurnRunner` source-boundary coverage, Planner JSON repair for prose/truncated object output, non-React `BuildTurnRuntime` boundary checks for approval/question/patch/terminal branches, `ToolCallExecutor` runtime approval execution, approved direct-tool lifecycle execution, and terminal-run lifecycle linkage, `ResumeController`, `SessionRestoreController` restore-preview diagnostics, and `CheckpointRestoreController` dry-summary/linked-action coverage, legacy runtime-boundary checks, Runtime item lifecycle coverage, Plan/Build mode tool contract coverage, typed ThreadEvent storage/projection/store restoration, `RuntimeLedger` / `RuntimeLedgerReducer` / `ActionRequiredStore` pending replay, ledger snapshot projection for pending actions / Inspector / run steps / checkpoint browser, non-React `ActionRequiredControllerCore`, `ReviewDockModel` ledger-only projection, ActionRequired question mirroring, default pending `resumeAction` creation, DeepSeek smoke harness, `SmokeRunController`, and three-path `DeepSeekSmokeRunner` run-record evaluation, PolicyEngine restrictive dynamic policy merging and persisted grant matching, PermissionScheduler install/network/grant classification, ActionRequired resolution/tool-result semantics, explicit WorkspaceGateway checks, user/workspace context rules with mode/glob/regex/policy filtering, external rule import candidates, read-only skill manifest discovery, Current Context Inspector and Context Memory model coverage, runtime ThreadEvent seam coverage for Agent run and patch workflow, structured ask_user question protocol, file action target normalization/runtime dispatch, rich file text rendering, legacy AgentEvent migration, persisted approval grant scope, file tree persistence, DeepSeek/provider capability fallback, credential vault prefix coverage, Composer paste attachment classification, context compaction events, recovered approval/question helpers, Inspector scoping, provider smoke state, thread isolation, and run-session resume/explicit-continue coverage. |
| `npm run build` | `verified` | TypeScript and Vite build passed on 2026-05-30 after the Runner Kernel tool-loop extraction; Vite still reports a Tauri API dynamic/static import chunk warning. |
| `cargo test --manifest-path src-tauri/Cargo.toml` | `verified` | 48 Rust tests passed, including Rust gateway search, Git shadow checkpoint restore/delete, sandbox patch preview, stale-write/path traversal coverage, transactional snapshot restore/delete rollback support, whitelisted workspace file open/reveal path validation, whitelisted Orbit context-file writing, provider host allowlist/model-list parsing, structured command cwd tests, credential vault encryption, Tauri dev workspace-root normalization, and SQLite legacy DB migration. |
| `npm run test:e2e` | `verified` | 43 Playwright tests passed, including real-workspace fixture, Plan natural-language planner draft with explicit “enter Build” acceptance, Current Context Inspector display/editing for `ORBIT.md` / `.orbit/rules`, Current Context DeepSeek smoke-gate evaluation, Settings Context user-rule management and mode filtering, project skill manifest visibility, file tree + Monaco preview, unified file action menu, Composer persistence, project permissions, multiple threads, long thread-history scrolling without pushing the file tree below the rail, thread archive/delete and clean new-thread isolation, settings layout, command approve/deny with unified `ActionRequiredOverlay`, install approval denial without terminal execution, structured ask_user question resolution through the same overlay, reload recovery for pending command/question/patch review, Ollama discovery-only Build blocking, sandboxed patch review, sandbox-preview retry after failure, transaction apply, conflict resolution, multi-file rollback restore/delete, file preview refresh, and verification approval. |
| `npm run tauri build -- --debug` | `verified` | macOS `Orbit Code.app` and `Orbit Code_1.0.0_aarch64.dmg` debug bundles were produced successfully on 2026-05-30 after the Runner Kernel tool-loop extraction, RuntimeLedger, ToolCallLifecycle, structured question, approval overlay, file action, Context Control Center, context inspector, Plan/Build mode-contract changes, and real DeepSeek smoke runner addition. |
| `npm run smoke:deepseek` | `verified` | Real DeepSeek API smoke passed on 2026-05-30 using Orbit trusted-device vault auto-unlock and `/Users/zhoujunjie/PersonalProjects/test for orbit/orbit-mini-lab`. The runner executed `happyPath`, `staleWriteRecovery`, and `contextPath`; each produced all required typed stages and `npm run test:run -> done (0)`. The script now retries transient empty/non-JSON model content once, uses a larger JSON response budget for stale-write recovery, and writes a failure ledger report instead of exiting without evidence. Evidence: `docs/smoke/deepseek-three-path-2026-05-30T07-37-57-035Z.json`. |

## Product Surface

| Area | Status | Evidence | Risk / Next Step |
| --- | --- | --- | --- |
| App shell | `verified` | Playwright app-load tests, independent settings workspace, Inspector toggle, and command palette smoke passed. | Keep layout stable while refactoring state. |
| DeepSeek smoke harness | `verified` | `src/runtime/deepSeekSmokeHarness.ts` evaluates required typed event milestones and produces `SmokeRunRecord` / `SmokeFailureRecord` details for missing stages. `SmokeRunController` adds a fixed mini-lab entry point and blocks fixture/provider runs from being marked successful. `DeepSeekSmokeRunner` defines reproducible happy-path, stale-write recovery, and context-path scenarios and aggregates them as `verified / partial / broken`. The Current Context Inspector exposes a semi-automatic “评估当前线程” gate over the live event/action snapshot. Unit and E2E coverage verify failure-ledger output, complete typed-loop recognition, fixture blocking, and UI reporting. Real DeepSeek manual smoke passed on 2026-05-29 for the scoped Plan -> Build -> patch -> verification -> summary path. `npm run smoke:deepseek` passed on 2026-05-30 against `/Users/zhoujunjie/PersonalProjects/test for orbit/orbit-mini-lab`: `happyPath`, `staleWriteRecovery`, and `contextPath` all produced `planDraft / modeSwitch / approval / question / patchProposal / checkpoint / verification / terminalRun / finalSummary` evidence and `npm run test:run` exited 0. | The 2026-05-30 runner is a local reproducible real-provider smoke that writes typed evidence and mini-lab files. Continue using desktop manual smoke for overlay UX details such as pointer/focus behavior. |
| Real workspace front end | `verified` | Desktop gateway fixture simulates a real opened project with a long file tree; Composer remains visible, files scroll independently, root folders start collapsed, file tree expansion persists per workspace, and permissions are editable. | Native folder picker exists for Tauri; keep fixture coverage for regressions. |
| File action menu | `verified` | File paths now route through a unified `FileActionTarget` / `FileActionMenu` path. File tree rows, Review Dock previews, diff file labels, and rich Timeline file references can open a controlled menu for VS Code, Cursor, system default app, Finder reveal, and copy path. Playwright covers the controlled VS Code action and copy-path flow. | VS Code/Cursor app availability is reported as a runtime failure when absent; native LaunchServices app enumeration is intentionally out of scope for v1. |
| Light/dark theme | `verified` | E2E theme toggle passed; light popover opacity and primary button hover contrast are covered. | Continue visual refinement, but readability regressions now have tests. |
| Chinese/English UI | `partial` | Main shell, sidebar, thread, output panel, settings, terminal, and diff copy now route through `src/i18n/copy.ts`; E2E language toggle passed. | Runtime/generated Agent messages and demo data still contain Chinese fallback strings. |
| Plan import | `verified` | Parser tests passed; E2E Plan import passed with Enter submit and Shift+Enter newline coverage. | Keep this flow green during frontend refactors. |
| Plan/Build mode contract | `verified for first pass` | Plan natural-language input now goes through a read-only planner path and produces a `Plan Draft` timeline event. The draft becomes a `CodingPlan` only after explicit “采纳并进入 Build”; Build remains the path for command approval, patch proposal, and verification. `PolicyEngine` now enforces Plan catch-all deny for executable tools and returns mode-denied tool results. Unit tests verify Plan registry excludes command/patch tools and E2E verifies Plan draft does not create approval or diff. | Next step is surfacing denied-mode attempts in Inspector history with stronger localized copy. |
| Task queue editing | `partial` | Imported tasks remain editable through the plan/task surfaces, but the Review Dock Tasks tab was removed from the main path so the dock can act as an Inspector. | Needs a dedicated center task-surface persistence test and task edit/delete/reorder user-flow tests. |
| Conversation/timeline | `partial` | Runtime events now have a pure `ThreadEventProtocol` that does not inherit from `AgentEvent`; legacy conversion remains in the snapshot migration/compatibility path. Timeline consumes typed `ThreadEvent[]` directly, renders Plan drafts and pending action chips, and no longer rebuilds its main display from legacy `AgentEvent` regex classification. Center timeline, pending actions, Inspector models, and run steps are selector-driven from the ledger snapshot instead of legacy approval/question queue inputs. `useAgentRun` and `usePatchWorkflow` now write through `emitThreadEvent/updateThreadEvent` instead of the legacy array setter. Thread menu, left-rail thread archive/delete, clean new-thread isolation, and Shift+Tab mode switching are covered. | Continue shrinking remaining compatibility helpers used by older modules such as embedding/self-heal/session restore; do not claim legacy storage is fully deleted until `AgentEvent` is migration-only. |
| Diff viewer | `verified for core flow` | Fixture provider creates a sandbox-previewed patch proposal in the center patch overlay and Inspector; E2E approves it, verifies transaction write, refreshes file preview, shows a verification command approval, and covers local-change conflict resolution before write. | Needs richer manual conflict editing and multiple-conflict coverage. |
| Settings/API Key UI | `partial` | Independent settings workspace, real section list, Models import surface, DeepSeek capability fallback, provider smoke state, Security matrix, Appearance preferences, Usage page, Orbit credential-vault unlock/store flow, and trusted-device auto-unlock UI are covered at the build/unit level. | Fixture smoke is automated; real provider model discovery still needs manual/API-key smoke after entering or auto-unlocking the vault. |
| Context Control Center | `verified for first pass` | Settings now has a Context section for global user rules with title/content/enabled/mode. Current Context Inspector can view and edit `ORBIT.md`, `.orbit/rules`, and `.orbit/rules.md`, shows active/disabled block match reasons, external rule import candidates, and project skill manifests, and refreshes after safe Rust-gateway writes. Unit and E2E cover mode filtering, workspace rule editing, skill discovery, external candidates remaining disabled, user-rule glob/regex/policy filtering, and `permissionImpact: none`. The 2026-05-30 real DeepSeek `contextPath` smoke created and inspected `ORBIT.md`, `.orbit/rules.md`, and `.orbit/skills/smoke/SKILL.md` while keeping context permission impact at none. | Other agents' rule files are only explicit import candidates; they are not auto-injected. Keep validating context visibility separately from permission expansion. |
| Provider persistence | `partial` | Provider settings and imported model metadata persist in SQLite; API keys now use Orbit's encrypted credential vault instead of OS Keychain. Trusted-device auto-unlock can be enabled explicitly for personal devices. | Existing OS Keychain entries are no longer auto-read; users must re-enter API keys once into the Orbit vault. |
| Composer attachments | `partial` | Paste classifier tests cover short text, code blocks, YAML plans, and long text; Composer now renders removable attachment chips and plan-import actions. | Needs Playwright coverage for paste/drop UI and binary attachment rendering. |

## Runtime And Agent

| Area | Status | Evidence | Risk / Next Step |
| --- | --- | --- | --- |
| Rust command registration | `verified` | `lib.rs` registers DB, file, shell, patch, LLM, embedding, window commands. The former flat `src-tauri/src/commands.rs` has been moved to `src-tauri/src/commands/mod.rs`, with `file / command / patch / context / provider / vault` child module entry points added while keeping existing Tauri command names stable. | Capabilities file is minimal; the child modules are currently extraction targets, not fully populated implementations. |
| SQLite schema/CRUD | `partial` | `db.rs` has multi-table schema and Rust tests pass indirectly. | Need migration/versioning story and integration tests. |
| Credential vault | `partial` | Rust encrypts API keys with Argon2id + AES-GCM, stores only ciphertext in SQLite, keeps decrypted keys in process memory after unlock, and supports an explicit trusted-device auto-unlock cache. | Needs manual desktop validation for restart/auto-unlock, disable auto-unlock, wrong-passphrase UX, and cross-platform filesystem-permission behavior. |
| File read/list/search | `partial` | Rust commands path-check reads and list workspace files; `WorkspaceGateway` now requires explicit `workspacePath` for runtime read/list/search/command calls; `search_code` routes through the Rust `search_workspace_files` gateway, using ripgrep when available and preserving Rust workspace validation. Frontend desktop gateway now restores the current workspace root and file tree on reload. | Some backend commands still rely on `std::env::current_dir()` compatibility fallback outside the Agent runtime path. |
| Shell execution | `partial` | Async/sync commands exist with process monitor; structured `{ command, args }` Rust tests pass. | Needs more integration coverage for long-running async commands and cancellation. |
| Approval policy | `verified` | Agent `run_command` now routes through `PolicyEngine` + `PermissionScheduler` before publishing a durable `ActionRequired` record. Plan-mode executable requests are denied without creating UI approvals; Build command/write/install/network decisions go through policy first, then the unified center overlay when user approval is needed. `PermissionScheduler.request()` returns a full `ActionRequiredResolution` with stable tool-result text for approve/deny/cancel/expired paths. Dynamic/project rule decisions cannot make an effective policy more permissive. Approval requests carry `once / session / project` grant scope plus mode/tool/action/path scope; persisted session/project grants are now formal `PolicyEngine` inputs, and Plan grants cannot expand Build write permissions. Active grants are visible and revocable in the Inspector. Unit tests cover install/network ActionRequired classification and grant matching, and E2E verifies install denial does not create terminal execution. The approval overlay now shows dedicated install/network risk copy. | Add E2E for grant revoke. |
| Transactional patch | `verified for core flow` | Rust transactional command and stale-write tests pass; Agent `propose_patch` creates a patch proposal, runs a temp sandbox preview, blocks real apply when sandbox fails, keeps failed previews pending for retry, creates a pre-apply checkpoint, and E2E verifies failed sandbox-preview retry, approved transaction write, conflict stop, conflict resolve, retry write, verification command card, and multi-file rollback restore/delete. Git workspaces now create an isolated shadow repo under the OS temp directory before patch apply; non-Git workspaces use `file-snapshot`. Rust rollback tests cover restoring edited files and deleting files created by a patch. Legacy `apply_patch` remains accepted as a Build-only compatibility alias. | Add broader manual rollback smoke in a real Git workspace. |
| Three-way merge | `verified` | Rust conflict tests passed. | Needs frontend E2E around conflict resolution UI. |
| Agent Loop | `runner-kernel tool-loop verified` | `AgentRunKernel` owns Build-turn preparation outside React state: Plan-mode guard, provider/model/key/build-support checks, imported-plan task selection, resume-run detection, run session id creation, and final-summary-only resume context. `AgentTurnRunner` now directly owns Build turn execution and uses `ToolLoopController` as the default non-React Build engine for model streaming, strict tool-envelope repair, mode-aware tool prompt/result feedback, denied-by-mode events, tool-result conversation feedback, context compaction, max-iteration failure, cancellation, and done/failure finalization. `agentLoopEngine.ts` is now a 56-line compatibility adapter over `ToolLoopController` instead of the owner of the ReAct loop. `BuildTurnRuntime` remains the non-React callback kernel for phase/stream projection, approval gates, structured `ask_user`, patch proposal sandbox preview, terminal run recording, context compaction display, and explicit-continue handoff. `useAgentRun.ts` remains the React adapter for UI state, provider inputs, start/continue/cancel, and runner result projection. Tool-call lifecycle entries (`generated / policyEvaluated / actionRequired / running / completed / failed / denied / cancelled`) write through `RuntimeLedger.appendToolCall/updateToolCall` via `ToolCallExecutor`; approved direct runtime tools now route through `ToolCallExecutor.executeApprovedTool`, command approval lifecycle writes route through `ToolCallExecutor.requestApproval`, and command terminal output is linked back to lifecycle records through `ToolCallExecutor.recordTerminalResult`. Non-React `SessionRestoreController` and `ResumeController` normalize restored command/question/patch/verification/terminal states into explicit continue results; restore preview now reports pending actions, last terminal recovered state, last checkpoint, last event summary, restorable/errors/warnings, and never resumes old Promises. Real DeepSeek three-path smoke passed through `npm run smoke:deepseek` on 2026-05-30. | Full automatic model continuation after app restart is intentionally not implemented. Recovered actions perform explicit local resume work, write events, and then wait for the user to continue. Next risk is deleting the thin `agentLoopEngine.ts` compatibility adapter and remaining legacy queue adapters once snapshot migration coverage is sufficient. |
| Controlled extensions | `verified for first pass` | User rules, `.orbit/rules`, `.orbit/rules.md`, `ORBIT.md`, accepted plan snapshots, record-only hooks, and skill manifests now have read-only context Module coverage. The Current Context Inspector shows source, mode, token estimate, matched rules, disabled blocks, skill parse errors, provider errors, and `permissionImpact: none`; these adapters inject context but do not grant tools or bypass PolicyEngine. The 2026-05-30 real DeepSeek `contextPath` smoke exercised Orbit rules and a project skill manifest without changing permission behavior. | Needs continued desktop UX validation as the Inspector evolves. |
| Semantic search / RAG | `partial` | `embedding.rs`, `semanticSearch.ts`, design spec exist; Rust embedding tests pass. | Requires OpenAI key and built index; `.gitignore` respect is not confirmed. |
| Multi-window | `partial` | Rust `window_manager.rs` and frontend button exist. | Needs desktop integration test/manual validation. |
| Packaging | `partial` | macOS debug `.app` and `.dmg` build verified on 2026-05-29. | Windows/Linux packaging remains manual/unverified. |

## Architecture Hotspots

| Module | Status | Problem |
| --- | --- | --- |
| `src/state/useWorkspace.ts` | `improved, still broad` | Session/file-system/project/layout/approval/patch/agent Modules exist, but the coordinator still wires many props and workflow callbacks. |
| `src/state/plannerEngine.ts` | `verified for first pass` | Natural-language Plan requests now run through a bounded read-only planner tool loop that can read/list/search and emit a `Plan Draft`; `useWorkspace.ts` gates credentials, records events, and accepts the draft into Build. |
| `src/features/thread/ThreadCanvas.tsx` | `improved, still partial` | Replaced the legacy `Conversation.tsx` path; central thread now composes Plan summary, Agent timeline, composer, and thread menu from focused Modules. Agent reasoning is visible by default for Plan and Build, can be disabled in Appearance settings, and collapses once the result event is written. Runtime event rendering still needs further view-model narrowing. |
| `src/components/thread/AgentTimeline.tsx` | `improved, still partial` | Timeline now consumes typed `ThreadEvent[]` directly for user messages, plans, command execution, patch proposals, questions, verification, and context compaction. It also projects lightweight pending action chips for approvals, questions, and patch reviews. It still owns sanitizer/display compaction and should keep shedding view-model logic. |
| `src/features/files/FileActionMenu.tsx` | `verified` | Centralized menu UI for file actions across Timeline, file tree, Review Dock preview, and diff surfaces. |
| `src/features/review/ReviewDock.tsx` | `improved, still partial` | Review Dock now behaves as an Inspector: file preview, patch diff details, checkpoint/rollback history, terminal records, and recent run details. Terminal history exposes process/session ids, cancellation/completion times, output tail fallback, and recovered state instead of showing restored long commands as still running. `ReviewDockModel` consumes a `RuntimeLedgerSnapshot` and no longer accepts approval/question queues as model inputs. Command/question/verification actions are handled by central overlays instead of duplicated dock queues. |
| `src/runtime/toolRegistry.ts` / `src/runtime/workspaceGateway.ts` | `improved, still partial` | `buildToolRegistry(mode)` now exposes separate Plan and Build registries. Plan only gets read/list/search/ask/done_plan; Build gets executable command, patch proposal, and done_build tools. Runtime read/search/command adapters require explicit `workspacePath` and no longer pass `workspacePath || ""`. Legacy aliases stay in runtime compatibility paths but are not exposed in prompts. | Continue moving mode policy out of prompt text and into a deeper runtime engine interface. |
| `src/state/agentRunKernel.ts` / `src/state/agentTurnRunner.ts` / `src/state/toolLoopController.ts` / `src/state/buildTurnRuntime.ts` | `runner-kernel tool-loop verified` | Non-React Runner extraction now owns Build-turn preparation, provider/build guards, task selection, resume detection, final-summary resume context, Build engine execution, structured Build turn results, approval/question/patch/terminal callback handling, stream event projection, strict tool-envelope repair, model/tool-result feedback, and done/error finalization. `AgentTurnRunner` defaults to `ToolLoopController`; `agentLoopEngine.ts` remains only as a compatibility adapter. `useAgentRun.ts` delegates this path and no longer imports `findProvider`, constructs `BuildAgentEngine`, or directly implements approval/question/patch/terminal tool branches. | Next cleanup is deleting the compatibility adapter and tightening tests around direct runtime tool execution through `ToolCallExecutor`. |
| `src/runtime/policyEngine.ts` / `src/runtime/permissionScheduler.ts` | `verified for first pass` | Policy and blocking permission decisions now live behind a runtime Seam instead of UI queue hooks. Persisted `PolicyGrant` records are now evaluated by `PolicyEngine` with workspace/mode/tool/action/path scope, and `PermissionScheduler` returns stable `ActionRequiredResolution` records. `useApprovalQueue` is legacy migration/compatibility surface, not the policy owner. |
| `src/runtime/contextProviders.ts` | `verified for first pass` | User rules, fixed Orbit project-rule files, accepted-plan context, and `.orbit/skills/*/SKILL.md` manifests are collected through a read-only provider registry and projected into the Current Context Inspector. User rules now support mode, globs, regex, and policy filtering, with match reasons visible in the Inspector. Rules and skills can guide model context but cannot change permissions. |
| `src/state/threadRuntimeStore.ts` | `verified for first pass` | `RuntimeLedger` now wraps event and `ActionRequired` writes, Runtime item lifecycle helpers (`appendItem`, `updateItem`, `completeItem`, `failItem`), tool-call lifecycle append/update/upsert records, terminal run records, checkpoint projections, pending replay, and snapshot serialization; `RuntimeLedgerReducer` is now the React state owner for events, actions, tool calls, terminal runs, and checkpoint runtime snapshots; `ThreadRuntimeStore` remains as a compatibility subclass. `useWorkspace` now keeps a single reducer-backed runtime state, publishes questions/approvals/patch reviews through a non-React `ActionRequiredControllerCore` adapter, writes tool lifecycles through the ledger, persists only `runtimeLedgerSnapshot + agentRunSession + approvalGrants` on the new save path, and derives pending actions, the unified overlay, Inspector details, terminal usage, and run steps from the ledger snapshot instead of the legacy approval/question merge path. |
| `src-tauri/src/commands/mod.rs` | `partial` | The old monolithic Rust command file is now a `commands` module with child-module entry points for file, command, patch, context, provider, and vault concerns while preserving public command registration. | Move implementations into those child modules incrementally and keep Agent runtime paths explicit about `workspacePath`. |
| `docs/*` | `partial` | Matrix, next tasks, known failures, and source-study notes were refreshed for the 2026-05-29 baseline. Keep older handoff docs subordinate to this file. |

## Definition Of Done For Next Handoff

- `npm test -- --run`, `npm run build`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run test:e2e`, and `npm run tauri build -- --debug` all pass for the claimed layer.
- `AGENTS.md`, `docs/GEMINI_DEEPSEEK_HANDOFF.md`, and this matrix agree.
- Any claimed feature has a concrete verification command or explicit manual-validation note.
- Frontend refactors reduce `useWorkspace`, `ThreadCanvas`, and Review Dock public Interfaces instead of only moving code around.
