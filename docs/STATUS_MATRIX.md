# Orbit Code Status Matrix

最近审查日期：2026-06-04

本文档是当前事实源。旧自研运行时、实验适配线和旧 session 恢复路线已经删除或降级为历史，不再作为产品状态依据。

## 验证基线

| 命令 | 状态 | 说明 |
| --- | --- | --- |
| `npm test -- --run` | `verified` | 2026-06-04 通过，46 files / 207 tests；包含 Codex delta reducer、runtime error/retry/reload 收尾、recover runtime 取消 active operation 并 interrupt running turn、reload 后 running approval/question 归一回 pending 且非交互 running command/terminal/file edit 失败化、desktop approval/question submit failure 可恢复错误投影、Composer foreground operation submit lock、interrupt operation 期间锁定并在终态释放、stale sidecar connection event 丢弃、Settings Build gate、Settings runtime control plane conformance display、Settings 模型能力 badge、direct Plan nested provider usage aggregation、workspace local usage snapshot 接入 Codex token usage、MarkdownText、Codex runtime boundary、Agent adapter conformance summary、provider import/gating、Together AI/Fireworks AI/Cerebras/NVIDIA NIM/Azure OpenAI/custom OpenAI-compatible discovery-only Build block、provider endpoint path normalization、结构化模型能力 metadata 导入、Ollama 作为本地 LLM provider 的非 Build 链路回归，以及 live vault export/bootstrap 脱敏闭环回归。 |
| `npm run build` | `verified` | 2026-06-04 通过，仅保留既有 Vite chunk size / dynamic import warning。当前前端以 Codex sidecar projection 驱动 Thread、Review Dock、Action overlay 和 runtime settings；Plan/普通聊天 route 为 `direct-deepseek-plan`，Build route 为 `codex-app-server-build`。 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | `verified` | 2026-06-04 通过，76 lib tests + AST/code graph/embedding integration tests；包含 Codex JSON-RPC 2.0 outbound request/result/error/notification normalization、fake app-server lifecycle response/notification/server-request interleaving fixture、app-server notification protocol method classification fixture、loopback bridge、no-secret config、`/v1/models`、missing credential、provider model-list variant/capability metadata parser、DeepSeek translation/SSE parser、DeepSeek bridge illegal role/tool-result sanitization、DeepSeek SSE mid-stream error rejection、direct Plan multi-provider stream error extraction、provider error secret redaction、Ollama NDJSON stream parser、workspace snapshot、sidecar lookup、Plan direct route、app-server timeout/failure cleanup、active Build recover cleanup、stable error item id、early app-server error/retry 绑定 Orbit turn id，以及 workspace patch stale-write 防护。 |
| `npm run test:e2e` | `verified` | 2026-06-04 通过，46 passed；Playwright fixture 已迁到 Codex thread/turn/item、approval deny、last-thread delete 和 legacy unsupported 行为，并覆盖桌面 Plan direct route、Settings recover input state for stuck desktop Plan operation、自定义 OpenAI-compatible Base URL gating、发送后 Thread Canvas 自动跟随底部、Build fixture approval/terminal/file edit/usage、pending approval/question/patch reload recovery。 |
| `npm run smoke:desktop-readiness` | `blocked on macOS / CI render wait broken` | 2026-06-04 CI run `26935563647` 证明 Ubuntu `tauri-driver`、`WebKitWebDriver`、DBus/Xvfb、session 创建和 `npm run tauri build -- --debug --no-bundle` 前置项均可用；debug binary build 已在 2 分钟左右完成。后续 packaged Plan smoke 第一次 `/execute/sync` 在应用仍显示 “正在加载本地 SQLite 数据库...” 时超时，artifact 中 stderr 仍只有 AT-SPI/DRI3 warning。Smoke runner 现在把早期 WebDriver execute 超时作为可重试轮询错误，并在最终失败时采集有限 DOM/URL/readyState 诊断；DB 连接也设置 `busy_timeout`，前端启动加载改为顺序读取 session/config，避免首次 schema/init 并发打到同一临时 SQLite。macOS 本机仍因 WKWebView 无 WebDriver 写入 blocked 报告。 |
| `npm run smoke:desktop-plan` | `blocked on macOS / CI launch smoke scaffolded` | 2026-06-03 本机运行仍写入 macOS blocked 报告。Linux/Windows 通过 `tauri-driver` 启动真实 Orbit Code Tauri binary 并验证 workbench shell 渲染；CI 上的 `/session` 创建现在使用 `ORBIT_WEBDRIVER_SESSION_TIMEOUT_MS`，默认至少 60s，并在报告中记录 request/session/overall timeout。设置 `ORBIT_DESKTOP_PLAN_LIVE=1` 后会进一步在真实窗口发送“你好”并检查没有 app-server 错误；默认 CI 不启用 live 模式，避免依赖本地 vault/API Key。 |
| `npm run smoke:desktop-build` | `blocked on macOS / live Ubuntu Build preflight hang` | 2026-06-04 CI run `26932972871` 证明完整 SQLite vault bundle 超过 GitHub environment secret 限制并失败在 `Bootstrap encrypted Orbit live vault`。本地随后把 `smoke:live-vault:export` 改为导出最小化 encrypted vault SQLite，只保留 `credential.vault.auto_unlock` 和 saved credential envelopes，bundle 约 22KB；`orbit-live-smoke/ORBIT_LIVE_VAULT_BUNDLE_B64` 已更新。CI run `26936384481` 已通过 vault/bootstrap/build/readiness/window/workspace load，但失败在 Build gate 的 “未配置模型 / Model unavailable”。CI run `26939495042` 证明 packaged app 启动前用 sqlite3 写入非敏感 `codex-sidecar.v1` DeepSeek `deepseek-v4-flash` session 后，模型配置、workspace load、desktop readiness、packaged Plan smoke、packaged Build readiness、Rust、frontend、E2E 均通过。approved live smoke 通过 gated `smoke-hook` 提交到 packaged workbench 后，8 分钟内仍未出现 approval，也未创建 smoke workspace 文件；WebDriver `/execute/sync` 和 `/source` 在 Build preflight 阶段持续超时。Smoke runner 已有 page-source fallback、原生 WebDriver element click fallback、短超时 session quit、SIGTERM/SIGKILL driver 回收、submit source 取证和 live 480s timeout；下一步需要在 Rust app-server/bridge/provider preflight 增加可落盘诊断或修复真实 Build 启动后无 UI 事件的问题。macOS 本机仍因 WKWebView 无 WebDriver 写入 blocked 报告。 |
| `npm run smoke:plan` | `verified` | 2026-06-04 通过，写入 `docs/smoke/latest-plan-stability-smoke.json`。该入口串联 `smoke:plan-route` 和 `smoke:plan-live`，作为普通聊天/Plan 的稳定门禁：既验证 workbench 提交 route 不触碰 Codex app-server，也验证真实 DeepSeek SSE 能返回增量。它仍不是 packaged Tauri window live Plan smoke。 |
| `npm run smoke:plan-route` | `verified` | 2026-06-04 通过，写入 `docs/smoke/latest-plan-route-smoke.json`。该入口封装聚焦 Playwright smoke，验证普通 Plan/chat 提交会调用 `codex_turn_start` + `runtimeMode=direct-deepseek-plan`，且发送后不调用 `codex_sidecar_status` 或 `codex_runtime_restart`；脚本现在用测试名过滤，不再依赖易漂移的行号参数。 |
| `npm run smoke:plan-live` | `verified` | 2026-06-04 通过，写入 `docs/smoke/latest-plan-live-streaming.json`。该入口从 Orbit trusted-device vault 内存解锁 DeepSeek credential，直接调用 DeepSeek chat SSE，验证 “你好” 可以返回 reasoning/content 增量且不经过 Codex app-server；不打印、不写入 API key。 |
| `npm run smoke:deepseek` | `contract smoke` | 2026-06-04 通过。默认入口执行 prepared Codex sidecar + app-server routing contract smoke，并写入 `docs/smoke/latest-codex-app-server-deepseek-smoke.json`。translation-only smoke 是 `npm run smoke:deepseek:bridge-unit`；旧直连 DeepSeek harness 是 `npm run smoke:deepseek:legacy`。 |
| `npm run smoke:deepseek:live-readiness` | `verified readiness` | 2026-05-31 通过，写入 `docs/smoke/latest-codex-live-readiness.json`。该入口验证 trusted-device vault 可非交互解锁、DeepSeek credential 存在、prepared Codex sidecar 存在、DeepSeek `/v1/models` 可达；不打印、不写入、不通过 env 传递 API key。它是 live Build smoke 的前置证据，不代表 approval -> terminal/fileEdit -> final summary 已完成。 |
| `npm run smoke:deepseek:live-app-server` | `verified live smoke` | 2026-05-31 通过并写入 `docs/smoke/latest-codex-live-app-server.json`。该入口启动真实 prepared Codex app-server、本地 Node Responses bridge、trusted-device vault DeepSeek credential，并验证 initialize、thread/start、DeepSeek live stream、approval request/response、terminal output、临时 workspace file edit、usage update、final assistant summary 和 turn completion。 |
| `npm run tauri build -- --debug --no-bundle` | `verified` | 2026-06-04 本地通过，beforeBuildCommand 会执行 `prepare:codex-sidecar` 和前端 build，并生成 `src-tauri/target/debug/orbit-code`。CI WebDriver smoke 使用该入口获得 Tauri CLI 构建语义但跳过 app/dmg/deb/msi bundling；生成的 `src-tauri/binaries/codex-*` 只作为本地 ignored artifact，不进入 Git。 |
| `codex exec` mock Responses SSE probe | `verified` | 2026-05-30 使用本机 `codex-cli 0.135.0-alpha.1` 指向 loopback mock `/v1/responses`，Codex 接受 `response.created`、`response.output_item.done`、`response.completed` SSE frames 并产出 `item.completed`/`turn.completed`。 |

## Runtime Matrix

| 区域 | 状态 | 当前事实 | 下一步 |
| --- | --- | --- | --- |
| Codex sidecar lifecycle | `partial` | Rust Codex command now prepares a pinned sidecar, starts one persistent `codex app-server --listen stdio://`, generates no-secret temporary config, routes JSON-RPC responses through pending channels, maps app-server notifications to fixed Tauri events, and caches `Orbit threadId -> Codex threadId` for the sidecar lifetime. The former monolithic `src-tauri/src/commands/codex.rs` is now a 12-line façade with physical shards under `src-tauri/src/commands/codex/` for types/state, JSON-RPC, Responses bridge, direct Plan, app-server lifecycle, notification mapping, turn commands, response translation, and tests. Build turns now use Codex `untrusted` approval policy so command execution goes through approval requests. Independent live smoke has proven real app-server initialize/thread/start, approval request/response, terminal output, temp file edit, usage, final assistant summary, and turn completion with DeepSeek traffic. Permissions approval requests now map to explicit Orbit approval metadata instead of a generic fallback. App-server failure/recover cleanup now clears stdin/child/thread cache/active context and releases every pending response/request, including active Build turns waiting on approval; operation snapshots are marked cancelled for user recovery and failed for sidecar failure, so sibling requests do not remain stuck after one timeout, disconnect, or recovery. Final app-server errors now use a stable per-turn item id, allowing frontend upsert to keep one terminal error card instead of appending repeated red cards. Early app-server errors/retries now retain the Orbit turn id until Codex returns an app turn id, so Build failures before `turn/started` stay attached to the active UI turn. | Verify the same active-turn crash/recover semantics through packaged UI with a deterministic sidecar crash harness, then run real desktop Build smoke. |
| Orbit provider bridge | `partial` | Bridge 绑定 `127.0.0.1:0` 并提供基础 HTTP loop：`GET /v1/models`、`POST /v1/responses`。DeepSeek `/v1/responses` 从 vault 内存读取 key，完成 history sanitization、DeepSeek chat completion translation、SSE reasoning/content/tool/usage frames。Rust bridge now has pure parser coverage for streamed DeepSeek reasoning/content/tool_call/usage aggregation, command-layer coverage verifies Codex orphan tool outputs, `tool_result`, `tool_call_output`, and unknown roles are converted into DeepSeek-safe user messages instead of illegal provider roles, SSE error frames now fail the stream with an explicit provider error instead of completing with partial content, and provider error text is redacted before unlocked API keys can reach UI diagnostics or logs. OpenRouter、Qwen/DashScope、SiliconFlow、Kimi、Groq、自定义 OpenAI-compatible 均有 catalog 和 blocked reason。Independent Node live bridge has proven the same DeepSeek credential can drive real Codex app-server approval, command output, usage, final summary, and file edit in a temp workspace. Packaged desktop Build smoke now exists and can run live via `ORBIT_DESKTOP_BUILD_LIVE=1`; macOS local run remains blocked by WebDriver limits. Rust DB/vault and live smoke scripts now support `ORBIT_APP_DATA_DIR` and minimized live vault bundles, so credentialed Ubuntu CI can bootstrap an isolated encrypted vault without exceeding GitHub secret limits. CI run `26939495042` proved the live job can pass vault bootstrap, no-bundle debug build, DeepSeek readiness, packaged window render, session seed, and isolated workspace load; remaining failure is that approved Build preflight never emits observable approval/tool events in packaged UI even after 480s. | Add Rust-side app-server/bridge preflight diagnostics for the packaged live path, then promote approve/deny desktop Build reports once approval and tool execution are visible. |
| Frontend runtime | `partial` | `useWorkspace.ts` remains a workbench facade and no longer imports old runner/tool loop. `useCodexSession` consumes `AgentRuntimePort`, event notifications, sidecar version info, restart result, recover result, and runtime settings model. Plan/ordinary chat now uses the direct provider streaming route (`direct-deepseek-plan`) and does not preflight, restart, or initialize the Codex app-server; this is covered by route-decision unit tests, fixture route smoke, live DeepSeek SSE smoke, Rust command-layer coverage for direct Plan provider stream error frames across OpenAI-compatible, Anthropic, Google, and Ollama-style payloads, and frontend projection coverage for nested usage metadata from DeepSeek/OpenAI-compatible, Anthropic, Gemini, and Ollama-style providers. The workspace local usage snapshot now consumes the same Codex usage projection, so Project Rail and Settings Usage count direct Plan/Build token usage instead of only terminal runs. Build is the only Codex app-server route; if the sidecar is not already ready, the effective workspace Build gate now becomes blocked before submission, so the RunControl/Settings state and the submitted turn agree on the same sidecar-not-ready reason instead of entering a half-running turn. Composer and `useCodexSession.submit()` now derive submit lock from runtime/active-operation state: foreground plan/build/interrupt operations block duplicate submissions, failed/interrupted/completed/cancelled terminal states restore submission, and stale sidecar connection events are ignored even when thread/turn ids match. `recoverRuntime` now uses a tested state helper to cancel active operations, interrupt running turns, and restore ready/stopped composer state without leaving a stale lock. Playwright now verifies Settings "Recover input state" cancels a stuck desktop Plan operation and re-enables a follow-up submission. Reload recovery now preserves interactive approval/question work by normalizing restored running items back to pending, while non-interactive running command/terminal/file edit items become failed with recovery metadata. Desktop approval/question submit failures no longer disappear silently: the action stays pending for retry/recovery and one recoverable runtime error card is upserted with non-secret action metadata. `src/runtime/agentRuntimeConformance.ts` now encodes the adapter promotion checklist and `codexRuntimeBoundary` verifies that Claude Code/OpenCode/Gemini remain isolated until they satisfy the same event, approval, file edit, usage, recovery, workspace, no-secret, and desktop smoke evidence. Settings runtime control plane now surfaces the production Codex Build core, evidence progress, missing evidence names, and alternative-agent spike/blocked status next to sidecar/bridge/smoke diagnostics. | Add broader state-machine tests for streaming/retry/reload recovery and run credentialed desktop Build smoke. |
| Thread projection | `verified for fixture loop` | `CodexItem -> RuntimeMessage / ThreadEvent / ActionRequired / TerminalRun / RunStep` 已覆盖 planDraft、fileEdit、usage、approval denied、question answered。 | 跟随 Codex app-server 原生 item schema 做兼容映射。 |
| Session schema | `verified for first pass` | `sessionStore` 改为 `codex-sidecar.v1`，旧 schema 不参与运行时恢复；E2E 覆盖 legacy session 不回放旧 runtime。 | 增加 legacy unsupported 的删除/归档 UX 打磨。 |
| Review Dock | `verified for fixture loop` | Review Dock 消费 Codex projection，E2E 覆盖 command approval、terminal、file edit、deny path 和 usage projection。 | 对接真实 DeepSeek bridge 后补真实 item 截图/人工验证。 |
| Provider registry | `partial` | 继续负责 metadata、model discovery、vault key mapping；Build gating 目前只放行 DeepSeek 和 fixture，其中 DeepSeek 还要求 vault unlock、model discovery、bridge smoke passed，并在 Settings runtime panel 中把 sidecar not ready 纳入 blocked reason。Rust bridge provider catalog 现在覆盖 OpenAI、Anthropic、Gemini、OpenRouter、xAI、Mistral、Groq、Qwen/DashScope、Kimi、SiliconFlow、Zhipu、Together AI、Fireworks AI、Cerebras、NVIDIA NIM、Azure OpenAI、Ollama 和 custom OpenAI-compatible；除 DeepSeek 外全部带明确 Build blocked reason。Ollama 现在也是 `LLMProvider`，legacy local `/api/chat` 非 Build 调用不再错误要求 vault key。Custom OpenAI-compatible 已支持用户填写 Base URL 后导入模型，Together AI、Fireworks AI、Cerebras、NVIDIA NIM 和 Azure OpenAI 已作为 OpenAI-compatible 端点加入 discovery-only 导入路径；远端 Base URL 必须 HTTPS，HTTP 仅允许 loopback；Azure OpenAI Base URL 必须是 HTTPS 且 host 属于 `*.openai.azure.com` 或 `*.services.ai.azure.com`，请求使用 `api-key` header；模型列表解析现在兼容 OpenAI/Anthropic `data`、常见私有网关 `models`、single model object、Google `supportedGenerationMethods` 过滤和 Ollama `name` 形态；新增结构化 `list_llm_model_infos` 导入路径，可保留 API 返回的 context window、max output、tool calling 等非敏感能力 metadata，同时前端仍强制沿用 DeepSeek-only Build gate；Settings 模型列表现在把上下文窗口、输出上限、工具调用、本地/API/官方来源显示为 compact badges，900px Playwright 检查无横向溢出；Base URL 粘贴完整 `/chat/completions`、`/responses`、`/messages`、`/api/chat`、`/models` 端点时会按导入或聊天目标自动归一化。 | 逐个验证 OpenAI/OpenRouter/Qwen/SiliconFlow/Kimi/Groq/Zhipu/Together/Fireworks/Cerebras/NVIDIA/Azure/custom 后解除 Build block。 |

## Cleanup Matrix

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 旧 runtime 文件删除 | `verified for source` | 旧 runner/tool-loop/实验适配器/tool-registry/policy-engine 文件已从 `src/` 删除。 |
| 旧 runtime import boundary | `verified for source` | 新 `codexRuntimeBoundary` 测试扫描生产 TS/TSX，防止旧核心符号回流。 |
| `useWorkspace.ts` 收敛 | `verified for first pass` | 当前 378 行，作为 workbench facade，不再直接执行模型 tool branching。 |
| 前端 tool JSON parsing | `verified for first pass` | 旧 raw tool envelope parser 已删除；UI 只消费 Codex projection。 |
| 旧 session restore | `verified for first pass` | 旧 snapshot/Promise/action replay 不再进入新 session store。 |
| 文档路线 | `verified for status source` | `STATUS_MATRIX.md` 已切换到 Codex sidecar 矩阵，并移除旧路线表述。 |
| Smoke 证据收口 | `verified for cleanup` | Smoke 脚本默认覆盖 `docs/smoke/latest-*.json`，不再生成无限 timestamp 历史；`ORBIT_SMOKE_KEEP_HISTORY=1` 时才额外写历史 JSON，且历史 JSON 被 `.gitignore` 忽略。旧 tracked timestamp smoke 已从工作树删除，准备归入 `smoke-docs-cleanup` 提交边界。 |

## Risk Register

| 风险 | 状态 | 处理 |
| --- | --- | --- |
| Codex app-server protocol drift | `open` | Codex binary/version 已 pin 到 bundled sidecar 路线；Rust 现在有 JSON-RPC 2.0 outbound normalization、JSON-RPC multiplexing、fake app-server lifecycle response/notification/server-request interleaving fixture，以及 app-server notification protocol method classification fixture，覆盖 initialize/thread/start/turn response、turn started/completed notification、assistant delta、approval server request、token usage、warning/error 等关键 Orbit 分类。下一步仍是扩成 fake app-server stdio lifecycle tests，验证 reader loop 对完整 stdout 序列和 pending response cleanup 的端到端行为。 |
| Provider bridge correctness | `mitigated` | DeepSeek HTTP loop 已补 Responses SSE 包装、history sanitization、streamed reasoning/content/tool_call/usage aggregation、tool result repair、mid-stream error rejection 和 provider secret redaction 单测，并通过 prepared sidecar contract smoke；Node live bridge 已用真实 DeepSeek credential 跑通 approval-required Codex app-server Build。Plan 稳定路径已从 app-server 解耦，`smoke:plan-live` 已验证普通 “你好” 聊天可以通过 DeepSeek SSE 增量返回，避免普通对话被 Build runtime 故障拖垮。下一步是桌面 UI 通过 Rust bridge 复现同一条 live Build 证据。 |
| E2E fixture drift | `mitigated` | Playwright fixture 已迁到 Codex item fixture；后续风险是 fixture 与真实 app-server item schema 漂移。 |
| Smoke parity | `verified for independent live driver` | `smoke:deepseek` 默认含义已切到 prepared sidecar + app-server routing contract smoke；`smoke:deepseek:live-readiness` 已证明本机可通过 trusted-device vault 非交互读取 DeepSeek credential 并访问 DeepSeek `/v1/models`，且 key 不进入 env/config/log。`smoke:deepseek:live-app-server` 已证明真实 Codex app-server + DeepSeek 可以完成 approval request/response、terminal output、usage、final summary 和临时 workspace 文件写入。旧直连 DeepSeek harness 仅作为历史 artifact。 | 把 independent live driver 覆盖的能力落到 Rust bridge 和桌面 UI Build 路径。 |
